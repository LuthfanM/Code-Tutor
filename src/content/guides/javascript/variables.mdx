---
title: "JavaScript Variables & Data Types"
description: "Belajar variable dan tipe data di JavaScript."
category: "javascript"
section: "JavaScript Basics"
slug: "variables"
order: 2
readingTime: "5 min"
updatedAt: "2026-06-09"
---

# JavaScript Variables

Variable dipakai untuk menyimpan data.

## const

Gunakan `const` kalau value tidak akan di-reassign.

```js
const name = "Luthfan"
console.log(name)
```

## let

Gunakan `let` kalau value bisa berubah.

```js
let count = 0
count = count + 1
console.log(count)
```

## Simple Rule

- Pakai `const` secara default.
- Pakai `let` hanya kalau nilainya memang perlu berubah.
